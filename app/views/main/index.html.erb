<h1> サンプル </h1>

<h2> 食べ物リスト </h2>

<table>
  <tr>
    <th> ID </th>
    <th> 名前 </th>
    <th> 値段 </th>
  </tr>

  <% @foods.each do |food| %>
    <tr>
      <td><%= food.id %></td>
      <td><%= food.name %></td>
      <td><%= food.price %></td>
    </tr>
  <% end %>


</table>